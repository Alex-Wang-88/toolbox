# TOOLBOX 前端按钮实现核查与修复报告

**日期**：2026-07-21
**场景**：前端代码审查 + 修复（按钮实现核查 → 落代码 → 代码审查 → 实测验证）
**参与成员**：调查员（gstack-investigator，修复执行）+ 产品评审员（gstack-product-reviewer，代码审查）

---

## 📌 TL;DR（执行摘要）
- 整体结论：🟢 通过（全部 5 项需改代码的问题已修复，代码审查 Go，关键后端改动已实测验证）
- 阻塞项数量：0
- 下一步：用户刷新浏览器（http://127.0.0.1:5000）实测前端按钮交互；建议补端到端冒烟测试

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go |
| 严重度分布（修复前） | 🔴 2（F1/F2） / 🟠 0 / 🟡 4（F3-F6） |
| 严重度分布（修复后） | 🔴 0 / 🟠 0 / 🟡 0 / 🟢 全绿 |
| 关键行动项 | 5 项已修复 |
| 建议负责人 | —（已完成） |

---

## 1. 各成员核心结论

### 🔧 排障手（调查员 / 修复执行）
- 核心判断：6 项问题（F1-F6）中 5 项需改代码、F4 仅标注；F1 残留调用是阻断主流程的头号根因。
- 关键动作：删除 `index.html:1934` 未定义函数调用（F1）；后端 `/api/output-folder` 补 POST 分支（F2）；`/api/generate` 请求体移除冗余字段（F3）；新增「下载视频」按钮 + `downloadVideo()`（F5）；`openVideo` 改锚定行首替换（F6）；F4 标注为后端预留。改动经 `node --check` 与 `py_compile` 通过。

### 🔍 产品官（产品评审员 / 代码审查）
- 核心判断：按 review skill 的 7 专家视角逐条核对改动，F1-F6 全部 ✅ 通过，无语法/运行时错误、无副作用、无新引入风险。
- 关键建议（非阻塞）：建议补端到端冒烟测试（generate→下载视频）；`/api/output-folder` 无鉴权在本地单用户桌面程序（127.0.0.1）威胁模型下可接受。

---

## 2. 综合审查发现（去重合并后按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 修复 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| F1 | 🔴(修复前) | 前端 JS | index.html:1934 | 调用未定义函数 `updateIndexVoiceRegisterButton()`，致 `updateControls()` 每次抛 ReferenceError，废掉「生成文稿/全部重生成/生成视频」三大按钮，并对回访用户造成整页初始化中断 | 删除该调用（整行） | 调查员→评审员 |
| F2 | 🔴(修复前) | 前后端不一致 | web_server.py:592 + index.html:1010 | `saveTypedOutputFolder` 向不存在的 `POST /api/output-folder` 发请求，后端仅 GET → 405，编辑输出目录弹报错且阻断生成 | 后端路由改 `GET,POST`，POST 解析 path（空值 400、保存后返回）；前端无需改 | 调查员→评审员 |
| F3 | 🟡 | 冗余字段 | index.html:1602/1604 | `POST /api/generate` 发送后端不读取的 `output_folder`/`tts_large_batch` | 移除两字段，去死变量 `lockedOutputFolder` | 调查员→评审员 |
| F4 | 🟡(观察) | 悬空路由 | web_server.py:1186 | `POST /api/voices/upload` 后端已实现、前端无 UI（注册音色功能本次不做） | 标注为后端预留能力，前端暂未开放（未改代码） | 调查员 |
| F5 | 🟡 | 未用路由 | index.html:1731/1765 + web_server.py:817 | 后端 `GET /api/download` 已实现，前端无浏览器直下载入口 | 新增「下载视频」按钮 + `downloadVideo()`（临时 `<a>` 触发下载） | 调查员→评审员 |
| F6 | 🟡 | 脆弱拼接 | index.html:1757 | `openVideo` 用 `openUrl.replace('/api','')` 全局替换，强依赖 open_url 必以 /api 开头 | 改 `openUrl.replace(/^\/api/, '')`，锚定行首、只去一次前导 /api | 调查员→评审员 |

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 删除 index.html:1934 残留调用 | 调查员 | 已完成 | 2026-07-21 |
| 2 | 后端 /api/output-folder 补 POST 分支 | 调查员 | 已完成 | 2026-07-21 |
| 3 | /api/generate 移除冗余字段 | 调查员 | 已完成 | 2026-07-21 |
| 4 | 新增「下载视频」按钮 + downloadVideo() | 调查员 | 已完成 | 2026-07-21 |
| 5 | openVideo 改锚定行首替换 | 调查员 | 已完成 | 2026-07-21 |
| 6 | 用户刷新浏览器实测前端按钮（生成文稿/生成视频/下载视频） | 用户 | P2 | 验证后 |
| 7 | 补端到端冒烟测试（generate→下载视频） | 待排期 | P3 | 后续 |

---

## ⚠️ 待完善 / 已知局限

- F2 后端 `save_output_folder` 仅写配置、不校验路径合法性；用户输入非法路径时 `get_output_folder()` 会 `makedirs` 创建目录（不崩溃），更强校验可后续补充。
- F4 为有意的产品预留（注册音色功能本次未实现），非缺陷。
- 前端交互（F1/F3/F5/F6）已通过 `node --check` + 代码审查 + 主理人亲核落位，但因无头环境无法模拟点击；建议用户在浏览器刷新后实测。
- 测试副作用：验证 F2 时曾用合法路径 POST，已将输出目录配置写为 `output/`（项目标准输出目录，无碍）。
- 项目无 git 仓库，无法 git diff/commit；改动以文件直接修改方式落地。
- 实测验证：重启 Flask（单实例，端口 5000 → PID 39108）后，`POST /api/output-folder`（空 path）返回 400、`GET /api/output-folder` 返回 200，证明 F2 后端修复真实生效；`GET /api/download/<task_id>` 路由命中（无任务时返回 500 而非 404/405），证明 F5 后端支撑就绪。

---

## 📚 成员产出索引

- gstack-investigator（调查员）原始产出：修复执行回报（F1-F6 改动说明、node/py 编译通过、git diff 不可用说明）
- gstack-product-reviewer（产品评审员）原始产出：代码审查报告（F1-F6 逐项 ✅、总体 Go、2 条非阻塞建议）

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
