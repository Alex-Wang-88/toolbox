# toolbax 项目说明书

> 一份覆盖安装环境、依赖工具、硬件需求与运行方式的完整手册。
> 适用版本：当前工作区（截至 2026-07-21）。操作系统以 **Windows 10/11（64 位）** 为主，macOS 为有限支持。

---

## 1. 项目简介

**toolbax** 是一个基于 Web 的「素材转讲解视频」工具。它接收图片、PDF、PPT/PPTX、Word 等素材，自动生成带 AI 讲解语音与字幕的视频，并提供语音克隆（本地 GPU）能力。

核心能力：
- 图片 / 文档（PDF、PPT、Word）→ 分页图片 → 讲解视频
- 多种 TTS 引擎：**Edge TTS**（在线，默认兜底）、**CosyVoice3**（本地 GPU 零样本语音克隆）
- 视频合成：MoviePy + 本地 FFmpeg，支持字幕、配音、进度回调
- 企业方案链接 → 智能体生成 Word → 复用文档转视频能力（命令行入口）
- 桌面启动器（PyInstaller 打包为 exe / py2app 打包为 .app）

---

## 2. 系统架构概览

```
┌─────────────────────────────────────────────────────────────┐
│  主进程 (Python 3.13.14 · 根 .venv)                            │
│  src/web_server.py (Flask) / app_launcher.py / toolbax.py     │
│  - 网页服务 / 图床上传 / 视频合成 / 硬件检测 / 任务调度          │
│  - 绝不 import torch / cosyvoice（延迟到子进程）               │
└───────────────┬───────────────────────────┬───────────────────┘
                │  stdin/stdout JSONL (IPC)   │  子进程调用
                ▼                             ▼
┌───────────────────────────┐   ┌──────────────────────────────────────┐
│ CosyVoice3 Worker          │   │ 文档/媒体处理                          │
│ (tts_poc/venv_cosyvoice)   │   │ - document_converter (PDF/PPT/DOCX)   │
│ torch 2.11.0+cu128         │   │ - ffmpeg (bin/ffmpeg, 已内置)          │
│ 模型: CosyVoice3-0.5B      │   │ - moviepy / imageio                   │
│ 占用 GPU (VRAM≥6GB)        │   │                                       │
└───────────────────────────┘   └──────────────────────────────────────┘
```

关键设计：主流程与重型的 TTS 推理**完全隔离**在两个 Python 虚拟环境里，通过标准输入/输出上的 JSONL 协议通信（见 `src/tts_worker_protocol.py`）。这样主进程无需装载 torch，也便于分别重建环境。

---

## 3. 安装环境（运行环境自包含）

本项目的运行环境**已内置在当前工作区内**，不依赖系统全局 Python。两个虚拟环境共享同一份项目内 Python 解释器。

### 3.1 基础 Python 解释器（已内置）

| 项 | 说明 |
|---|---|
| 路径 | `runtime/python/` |
| 版本 | Python 3.13.14（CPython，官方发行版整目录复制，可重定位） |
| 体积 | 约 316 MB |
| 备注 | 两个 venv 的 `pyvenv.cfg` 的 `home` 均指向此处；删除它会导致两个 venv 无法运行 |

> 历史说明：该解释器最初分别来自 Windows 应用商店 Python 与 WorkBuddy 受控 Python，现已统一复制进项目，实现自包含。

### 3.2 两个虚拟环境

| 虚拟环境 | 路径 | 用途 | 依赖清单 |
|---|---|---|---|
| 主服务 venv | `.venv/` | 网页服务、文档转换、视频合成、Edge TTS | `requirements.txt`（59 个包） |
| CosyVoice3 venv | `tts_poc/venv_cosyvoice/` | 本地 GPU 语音克隆推理（torch/cu128） | `tts_poc/requirements.txt`（133 个包） |

主服务 venv 关键依赖（节选）：`Flask`、`flask-cors`、`numpy`、`onnxruntime`、`requests`、`edge-tts`、`moviepy`、`imageio-ffmpeg`、`python-docx`、`PyMuPDF(fitz)`、`faster-whisper`、`ctranslate2`、`av`、`pysrt`、`modelscope`、`huggingface_hub`、`tokenizers`。

CosyVoice3 venv 关键依赖（节选）：`torch==2.11.0+cu128`、`torchaudio`、`transformers`、`modelscope`、`pytorch-lightning`、`x-transformers`、`soundfile`、CosyVoice 引擎（vendored，非 pip 包）。

> 重建命令（见第 9 节）。

### 3.3 FFmpeg（已内置）

| 项 | 说明 |
|---|---|
| 路径 | `bin/ffmpeg/ffmpeg.exe` 与 `bin/ffmpeg/ffprobe.exe` |
| 体积 | 约 434 MB（两个可执行文件各约 227 MB） |
| 解析优先级 | ① `LOCAL_TTS_FFMPEG_PATH` 环境变量指定 → ② exe 同目录 / PyInstaller 解包目录 → ③ 系统 `PATH` 上的 ffmpeg → ④ 退化用裸 `ffmpeg` |

无需在系统 PATH 安装 FFmpeg。

### 3.4 模型权重（已内置，体积最大）

| 项 | 路径 | 体积 |
|---|---|---|
| CosyVoice3 语音克隆模型 | `tts_poc/models/CosyVoice3-0.5B/` | **约 9.1 GB** |
| 关键文件 | `llm.pt`、`flow.pt`、`hift.pt`、`speech_tokenizer_v3.onnx`、`campplus.onnx`、`cosyvoice3.yaml` | — |

运行时设置 `MODELSCOPE_OFFLINE=1`、`HF_HUB_OFFLINE=1`、`TRANSFORMERS_OFFLINE=1`，**首次运行无需联网下载模型**。

### 3.5 第三方工具（系统级，需自备）

| 工具 | 用途 | 是否必须 |
|---|---|---|
| **NVIDIA 显卡驱动** | CosyVoice3 GPU 推理（CUDA 12.8 运行库随 torch wheel 自带，但驱动需本机安装） | GPU 语音克隆必须；无 GPU 时自动改用 Edge TTS |
| **Microsoft Visual C++ 2019+ 运行库** | torch / onnxruntime 等原生扩展依赖 | 必须（Windows 大多已预装） |
| **LibreOffice**（或 PowerPoint） | PPT/PPTX → 图片转换的跨平台兜底方案（Windows 优先用 PowerPoint COM） | 处理 PPT 时建议安装；PDF/DOCX 由 Python 库处理 |
| **网络** | Edge TTS 在线合成、AI 话术 API 调用 | 使用在线能力时必须；纯本地视频/语音克隆可离线 |

---

## 4. 用到的工具与技术栈

**前端**：HTML5 / CSS3 / 原生 JavaScript（无框架），页面在 `static/index.html`。

**后端**：Python 3.13 + Flask + flask-cors。

**TTS 引擎**
- `edge-tts`：微软在线 TTS，默认与兜底引擎（需联网）。
- `CosyVoice3`（0.5B，阿里开源）：本地零样本语音克隆，运行在独立 venv 的常驻 Worker 子进程，通过 JSONL 协议与主进程通信。支持参考音频 + 参考文本（ref_text）克隆音色。
- `IndeXTTS2`：曾在实验中探索（`indextts2_cleanup_delivery_20260718.md`），当前主链路未启用，CosyVoice3 为统一克隆引擎。

**语音识别 / 预处理**：`faster-whisper`（基于 `ctranslate2`），用于音频转写等辅助流程。

**视频与媒体**
- `moviepy` + `imageio` / `imageio-ffmpeg`：视频剪辑与渲染。
- `ffmpeg` / `ffprobe`（内置）：转码、封装、探针。
- `av`（PyAV）：音视频底层处理。
- `pysrt`：字幕（SRT）读写。

**文档转换**（`document_converter.py`）
- 图片：直接作为分页素材。
- PDF：`PyMuPDF (fitz)` 渲染为图片。
- DOC/DOCX：`python-docx` 提取文本后排版为图片页。
- PPT/PPTX：Windows 优先 PowerPoint COM，否则 LibreOffice 兜底转图。

**AI 话术**：通过 HTTP 调用外部对话式 AI API（见 `.env` 配置），未配置 Key 时程序用兜底话术，便于测试合成流程。

**打包发布**：PyInstaller（Windows exe）/ py2app（macOS .app），详见 `docs/BUILD.md`。

---

## 5. 硬件需求

### 5.1 最低配置（仅主流程 + Edge TTS 在线模式）
- **CPU**：双核及以上（x86-64）
- **内存**：8 GB
- **磁盘**：约 4–6 GB 可用（不含模型权重；含主 venv + ffmpeg + 运行时）
- **GPU**：不需要
- **系统**：Windows 10/11 64 位（macOS 有限支持）
- **网络**：使用 Edge TTS 与 AI 话术时必须联网

### 5.2 推荐配置（启用本地 GPU 语音克隆）
- **CPU**：4 核及以上
- **内存**：16 GB 及以上
- **GPU**：**NVIDIA 显卡，显存 ≥ 6 GB**，支持 CUDA 12.8（计算能力 8.0+，如 RTX 30 / 40 系列）
- **磁盘**：**完整环境约 14–16 GB 可用空间**（其中模型权重 `CosyVoice3-0.5B` 占 9.1 GB，两个 venv 约 4 GB，FFmpeg 约 0.4 GB，解释器约 0.3 GB，运行时数据另计）
- **驱动**：安装对应版本的 NVIDIA 驱动（含 CUDA 12.8 兼容运行库）

### 5.3 硬件门禁逻辑
- GPU 克隆能力由 `src/gpu_setup.py` 用 `nvidia-smi` 判定：**必须有 NVIDIA GPU 且显存 ≥ 6 GB**。
- 无 GPU / 显存不足时，语音克隆自动降级为 Edge TTS（在线）。
- 主流程可通过环境变量 `APP_VARIANT=cpu` 强制禁用 GPU 加速（仅影响主流程；CosyVoice3 仍需 GPU）。

---

## 6. 配置（环境变量）

参考 `config/.env.example`，把真实值写入项目根目录的 `.env`（程序启动时自动加载）：

| 变量 | 说明 | 默认值 |
|---|---|---|
| `TOOLBAX_API_KEY` | AI 话术 API Key（未设置则用兜底话术） | 空 |
| `TOOLBAX_API_URL` | AI 话术 API 地址 | 示例地址 |
| `TOOLBAX_OUTPUT_FOLDER` | 输出目录 | `output` |
| `SOLUTION_AGENT_API_KEY` | 企业方案智能体 Key | 空 |
| `SOLUTION_AGENT_API_URL` | 企业方案智能体地址 | 示例地址 |
| `SOLUTION_AGENT_TIMEOUT` | 智能体等待秒数 | 360 |
| `SOLUTION_AGENT_RETRIES` | 智能体重试次数 | 2 |
| `APP_VARIANT` | `gpu` / `cpu`，主流程是否启用 GPU | `gpu` |
| `COSYVOICE_VENV` / `COSYVOICE3_VENV` | 显式指定克隆引擎的 python.exe 路径（覆盖默认 venv） | 空 |
| `LOCAL_TTS_FFMPEG_PATH` | 显式指定 ffmpeg 完整路径（委派给子进程） | 空 |
| `MULTI_TTS_WORKER_LOG` | Worker stderr 日志路径 | 系统临时目录 |

---

## 7. 启动方式

### 方式一：启动脚本（Windows）
双击 `config/start.bat`，或命令行：
```bat
config\start.bat
```
脚本会做三件事：检查/安装主 venv 依赖 → 检查 FFmpeg → 启动 Flask 服务。访问 `http://localhost:5000`。

### 方式二：直接启动 Web 服务
```bat
runtime\python\python.exe src\web_server.py
:: 或（若已在 PATH 中的 3.13）
python src\web_server.py
```
访问 `http://localhost:5000`。

### 方式三：桌面启动器（开发态）
```bat
runtime\python\python.exe src\app_launcher.py
```

### 方式四：企业方案转视频（命令行）
```powershell
$env:SOLUTION_AGENT_API_KEY="your_key"
runtime\python\python.exe src\enterprise_solution_to_video.py "https://example.com/source-plan.pdf"
```
产物保存在 `solution_video_runs/<运行时间>/`。

### 方式五：打包为独立 exe（发布）
```bat
runtime\python\python.exe -m PyInstaller --noconfirm --clean config\toolbax.spec
:: 产物：dist\toolbax\toolbax.exe
```
详见 `docs/BUILD.md`。

---

## 8. 目录结构（关键部分）

```
toolbax/
├── runtime/python/            # 基础 Python 解释器（已内置，3.13.14）
├── .venv/                     # 主服务虚拟环境（Flask/视频合成等）
├── requirements.txt           # 主服务依赖清单（59 包）
├── tts_poc/
│   ├── venv_cosyvoice/        # CosyVoice3 推理虚拟环境（torch cu128）
│   ├── requirements.txt       # CosyVoice3 依赖清单（133 包）
│   ├── CosyVoice/             # CosyVoice 引擎源码（vendored，含 Matcha-TTS）
│   ├── models/CosyVoice3-0.5B/ # 语音克隆模型权重（约 9.1 GB）
│   └── download_model.py      # 模型下载脚本（离线环境已就绪可跳过）
├── bin/ffmpeg/                # FFmpeg / ffprobe（已内置）
├── src/                       # 源代码
│   ├── web_server.py          # Flask Web 服务
│   ├── app_launcher.py        # 桌面启动器
│   ├── toolbax.py             # 视频生成核心
│   ├── document_converter.py  # 文档→图片
│   ├── multi_tts_voice.py     # 多引擎 TTS Worker 客户端
│   ├── tts_workers/cosyvoice3_worker.py # CosyVoice3 Worker 子进程
│   ├── gpu_setup.py           # GPU 门禁与依赖校验
│   ├── gpu_arbiter.py         # 跨进程 GPU 串行锁
│   ├── hardware_profile.py    # 硬件检测 / 性能基准
│   └── ffmpeg_util.py         # FFmpeg 路径解析
├── static/index.html          # 前端页面
├── config/                    # .env.example / start.bat / toolbax.spec
├── docs/                      # README / BUILD / TESTING / plan
├── output/                    # 生成的视频（运行时）
├── app_data/                  # 运行时数据（缓存、音色注册、GPU 锁等）
└── audio_assets/              # 参考音频等素材
```

---

## 9. 环境重建与排错

### 9.1 重建主服务 venv
```bat
runtime\python\python.exe -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

### 9.2 重建 CosyVoice3 venv
```bat
runtime\python\python.exe -m venv tts_poc\venv_cosyvoice
tts_poc\venv_cosyvoice\Scripts\python.exe -m pip install -r tts_poc\requirements.txt
```
> 该 venv 的 `pyvenv.cfg` 的 `home` 必须指向 `runtime/python`，否则会因找不到标准库而无法启动。

### 9.3 常见问题

**Q：启动后本地语音克隆不可用？**
- 检查 `nvidia-smi` 是否能识别显卡且显存 ≥ 6 GB。
- 检查 `tts_poc/models/CosyVoice3-0.5B/` 权重文件是否齐全（见 3.4）。
- 检查 `tts_poc/venv_cosyvoice/Scripts/python.exe` 是否存在。
- 无 GPU 时会自动降级为 Edge TTS（在线）。

**Q：生成视频报 FFmpeg 相关错误？**
- 确认 `bin/ffmpeg/ffmpeg.exe` 存在；或用 `LOCAL_TTS_FFMPEG_PATH` 指向系统 ffmpeg。
- 若用打包 exe，FFmpeg 由 `imageio-ffmpeg` 内嵌，一般无需额外处理。

**Q：提示缺少 VC++ 运行库 / torch 加载失败？**
- 安装 Microsoft Visual C++ 2019+ 运行库（x64）。

**Q：想完全离线运行？**
- 模型已本地就绪且设置了 `*_OFFLINE=1`；只需保证不调用 Edge TTS 与 AI 话术 API 即可离线生成视频（使用本地克隆或兜底话术）。

**Q：换机器迁移？**
- 复制整个项目目录，确保目标机满足第 5 节硬件/系统依赖（NVIDIA 驱动、VC++ 运行库）。`runtime/python` 与两个 venv 已随项目携带，无需重新安装 Python。

---

## 10. 已知限制

- CosyVoice3 本地克隆**强依赖 NVIDIA GPU（显存 ≥ 6 GB）**，无 GPU 时只能用在线 Edge TTS。
- 完整环境磁盘占用大（模型权重 9.1 GB 为主），分发时请注意空间。
- macOS 支持有限：TTS 失败不兜底、PPT 转换依赖 LibreOffice、打包需 macOS 本机。
- 主服务依赖外部 AI 话术 API；未配置 Key 时仅能用兜底话术测试流程。

---

*文档生成日期：2026-07-21。环境结构以当前工作区实测为准。*
